# [React + MobX + TS + WebPack]

Install all dependencies

  ```sh
  (
    npm install
  )
  ```

Run app
  ```sh
  (
    npm start
  )
  ```


All you want to review is 

    ```sh
    (
     src/app  <--- all sits here
     also cinfig files in main directory
    )
    ```

